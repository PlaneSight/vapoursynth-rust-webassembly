[package]
name = "vapoursynth-core"
version = "0.0.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
vapoursynth-sys = { path = "../vapoursynth-sys" }

[lib]
crate-type = ["rlib", "staticlib"]

[lints]
workspace = true
