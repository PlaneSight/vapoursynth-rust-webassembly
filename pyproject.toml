[project]
name = "vapoursynth-wasm-build"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "cython==3.2.9",
    "meson==1.3.2",
]

[tool.uv]
default-groups = ["dev"]
package = false
