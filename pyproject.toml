[project]
name = "vapoursynth-rust-webassembly"
version = "0.1.0"
description = "Browser-oriented WebAssembly integration for the upstream VapourSynth core."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "meson==1.3.2",
]

[tool.uv]
default-groups = ["dev"]
