#!/usr/bin/env bash
set -euo pipefail

program="${1:?expected an executable path}"
shift

case "$program" in
    /*) ;;
    *) program="$PWD/$program" ;;
esac

cd "$(dirname "$program")"
exec node -e 'require(process.argv[1])' "./$(basename "$program")" "$@"
